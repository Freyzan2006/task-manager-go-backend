# .air.toml
root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/main.go"
  bin = "tmp/main"
  full_bin = "tmp/main -mode=debug -db=test.db -port=8000"
  include_ext = ["go", "yaml", "toml", "json"]
  exclude_dir = ["assets", "tmp", "vendor", "docs"]

[log]
  time = true
